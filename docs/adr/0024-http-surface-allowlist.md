# v1 HTTP surface allowlist

The AS HTTP API allowlist is: Authorization Endpoint, Token Endpoint, JWKS document, Resource Owner login and Consent form endpoints, and static assets required for those screens. No RFC 8414 authorization-server metadata document in v1. No other public routes.
