# Argon2id for passwords and client secrets

End-User passwords and Confidential Client secrets are hashed with Argon2id at rest. Other algorithms are out of v1 for these secrets. Fixed parameters (not Operator-tunable) are ADR-0053.
