# Sample Resource Server is test-only

End-to-end tests may include a minimal Sample Resource Server to prove Access Tokens are usable. That component is not a product feature and must not blur the AS-only product boundary (ADR-0001).
