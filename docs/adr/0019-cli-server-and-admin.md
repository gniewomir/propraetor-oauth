# Single CLI: server and admin subcommands

One binary serves both roles. `cli server …` runs the Authorization Server process. Admin subcommands (e.g. Client / User (End-User) / Scope management) are separate commands on the same CLI. There is no remote Operator HTTP admin API in v1 unless later named.
