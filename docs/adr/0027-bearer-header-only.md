# Bearer Tokens via Authorization header only

Per RFC 6750 usage in this project, Access Tokens are Bearer Tokens accepted only as `Authorization: Bearer`. Form-body and query-parameter bearer presentation are rejected (including by the Sample Resource Server).
