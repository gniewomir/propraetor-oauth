## Agent skills

### Issue tracker

Issues live in GitHub Issues for `gniewomir/propraetor-oauth` (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.

### Coding standards

How code should be written. See `CODING_STANDARDS.md`.

### Testing

Use `scripts/test-unit.sh`, `scripts/test-integration.sh`, and `scripts/test-e2e.sh`. See `docs/agents/testing.md`.
